import { MainClass } from './main-class';

export class BillingPadConsecutive extends MainClass {
  resolution: string;
  initial_consecutive: number;
  final_consecutive: number;
  actual_consecutive: number;
  expiracy_date: Date;
  status_id: number;
  billing_pad_prefix_id: number;

}
