import { MainClass } from './main-class';

export class GlossConciliation extends MainClass {
  id: number;
  gloss_id: number;
  repeated_initial_id: number;
  conciliations_date: number;
  objeted_value: number;
  user_id: number;
 
}
