import { Time } from '@angular/common';
import { SingleSeries } from '@swimlane/ngx-charts';
import { MainClass } from './main-class';

export class FixedLocationCampus extends MainClass {
  id: number;
  flat_id: number;
  campus_id: number;
  fixed_area_campus_id: number; 
}
